# v1.1 Product Changes — Summary

Cross-cutting spec for the next implementation slice. Update backend + frontend together.

## Features

| Change | Product doc | Backend | Frontend |
|--------|-------------|---------|----------|
| Per-category low stock threshold | [categories.md](../features/categories.md) | `Category.low_stock_threshold`, seed defaults, `updateCategory` | Settings → Categories; badges use threshold |
| Component grouping | [component-groups.md](../features/component-groups.md) | **Deferred** | **Deferred** |
| Resource link (URL only) | [components.md](../features/components.md) | Keep `datasheetUrl` | Label **Resource link** in UI |
| Stock reallocation | [storage-locations.md](../features/storage-locations.md), [inventory-logs.md](../features/inventory-logs.md) | `REALLOCATE` log type, `from_box` column | Move stock dialog on detail |
| Central activity log | [activity-log.md](../features/activity-log.md) | `inventoryLogs` query | `/activity` nav + page |
| Categories in Settings | [categories.md](../features/categories.md) | — | Remove `/categories`; Settings tab |
| UI compact cards + table rows | [architecture.md](../frontend/architecture.md) | — | StatCard, DataTable styling |

## API reference

Full GraphQL shapes: [api-design.md](./api-design.md)

## Migration notes (backend)

1. Aerich migration: `categories.low_stock_threshold`, `components.group_name`, `inventory_logs.from_box`, enum value `REALLOCATE`
2. Backfill category thresholds for existing users
3. Keep GraphQL field `datasheetUrl` — UI shows **Resource link** (no API rename in v1.1)

## Grill decisions (locked)

| # | Decision |
|---|----------|
| Grouping | Deferred |
| Reallocation UI | Separate **Move stock** dialog (not inside Record change) |
| Category thresholds | Edit on all categories; **rename blocked** for seeded defaults |

## Open decisions

None for current v1.1 slice. Grouping deferred to a later release.
